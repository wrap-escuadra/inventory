{#<div class="row">#}
{#<div class="col-xs-offset-1 col-xs-10">#}
    {{ form_start(form) }}

    {{ form_widget(form) }}
    <button class="btn btn-info pull-right">{{ button_label|default('Save') }}</button>

    {{ form_end(form) }}
{#</div>#}
{#</div>#}
